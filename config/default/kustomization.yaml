apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: antimetal-system

namePrefix: antimetal-

resources:
  - ../rbac
  - ../agent

patches:
  - path: agent_auth_proxy_patch.yaml
