apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: antimetal-system

resources:
  - ../rbac
  - ../agent

patches:
  - path: agent_auth_proxy_patch.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/instance: antimetal
    app.kubernetes.io/managed-by: kustomize
